spring.datasource.url=${TEST_DB_URL:jdbc:postgresql://localhost:5432/minibank_test}
spring.datasource.username=${TEST_DB_USERNAME:${USER:postgres}}
spring.datasource.password=${TEST_DB_PASSWORD:}
spring.datasource.hikari.maximum-pool-size=12

app.jwt.secret=test-only-secret-that-is-at-least-32-bytes
app.demo.enabled=false
app.admin.email=
app.admin.password=
